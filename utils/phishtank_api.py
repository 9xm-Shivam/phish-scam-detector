# utils/phishtank_api.py
import requests

# NOTE: This is a placeholder method. For production use, register with PhishTank/OpenPhish
# and implement API calls / local DB checks as per their documentation.

def check_phishtank_local(url):
    """
    Placeholder: returns False (not found) by default.
    Replace with real check against PhishTank DB/export if needed.
    """
    return {"in_database": False, "url": url}
